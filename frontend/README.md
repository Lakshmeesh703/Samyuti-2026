# Frontend (Vite + React)

Run:

```bash
npm install
npm run dev
```

Features:
- Verse input (IAST/Devanagari)
- Analyze JSON output viewer
- Chant audio generation
- Audio controls: unit duration, tonic frequency, glide, brightness, tonic drone
- Speech TTS provider toggle: Edge (default) / OpenAI (API key)
